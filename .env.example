# 环境变量配置示例文件
# 复制此文件为 .env 并根据需要修改配置

# 是否包含当前版本文档
# 开发环境设置为 true，生产环境设置为 false
# 如果不设置此变量，系统会根据 NODE_ENV 自动判断：
# - development 环境默认为 true
# - production 环境默认为 false
INCLUDE_CURRENT_VERSION=true

# 其他可能需要的环境变量可以在这里添加
# NODE_ENV=development